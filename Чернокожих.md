## Задача #74

SELECT<br>
&emsp;id,<br>
&emsp;CASE<br>
&emsp;&emsp;WHEN has_internet = TRUE THEN 'YES'<br>
&emsp;&emsp;ELSE 'NO'<br>
&emsp;END AS has_internet<br>
FROM<br>
&emsp;rooms;

===========

## Задача #56

DELETE FROM Trip<br>
WHERE town_from = 'Moscow';

===========

## Задача #114

SELECT DISTINCT p.name<br>
FROM flights f<br>
JOIN pilots p ON f.second_pilot_id = p.pilot_id<br>
WHERE f.destination = 'New York'<br>
AND f.flight_date BETWEEN '2023-08-01' AND '2023-08-31';

===========

## Задача #19

SELECT DISTINCT fm.status<br>
FROM FamilyMembers fm<br>
JOIN Payments p ON fm.member_id = p.family_member<br>
JOIN Goods g ON p.good = g.good_id<br>
JOIN GoodTypes gt ON g.type = gt.good_type_id<br>
WHERE g.good_name = 'potato';

===========

## Задача #21

SELECT g.good_name<br>
FROM Payments p<br>
JOIN Goods g ON p.good = g.good_id<br>
GROUP BY g.good_id, g.good_name<br>
HAVING COUNT(p.payment_id) > 1;

===========